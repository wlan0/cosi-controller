# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/sigs.k8s.io.my.domain_buckets.yaml
- bases/sigs.k8s.io.my.domain_bucketcontents.yaml
- bases/sigs.k8s.io.my.domain_bucketclasses.yaml
- bases/sigs.k8s.io.my.domain_bucketaccesses.yaml
- bases/sigs.k8s.io.my.domain_bucketaccessrequests.yaml
- bases/sigs.k8s.io.my.domain_bucketaccessclasses.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_buckets.yaml
#- patches/webhook_in_bucketcontents.yaml
#- patches/webhook_in_bucketclasses.yaml
#- patches/webhook_in_bucketaccesses.yaml
#- patches/webhook_in_bucketaccessrequests.yaml
#- patches/webhook_in_bucketaccessclasses.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_buckets.yaml
#- patches/cainjection_in_bucketcontents.yaml
#- patches/cainjection_in_bucketclasses.yaml
#- patches/cainjection_in_bucketaccesses.yaml
#- patches/cainjection_in_bucketaccessrequests.yaml
#- patches/cainjection_in_bucketaccessclasses.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
